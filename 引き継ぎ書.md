# 引き継ぎ書（縦スクロール・シューティングゲーム）

> このファイルは「新しいセッションに作業を引き継ぐためのメモ」です。
> 次に作業を始めるとき、まずここを読めば「今どこまで進んでいて・どう動かして・次に何ができるか」が分かります。
> 専門用語には *(かんたんな説明)* を添えています。

最終更新：2026-07-04（**画質・軽さ・遊びやすさの改善**を実施＝改良53〜59。**ポーズ画面から「タイトルへ戻る」**（Tキー・タップ）／**Retina対応**（高精細画面のにじみ解消）／**背景ぼかしのキャッシュ化**（低スペック端末の処理落ち対策）／**キャベツの救済**（8体で必ず出る）／**コンティニュー機能**／**音あり・音なしの切り替え**／**タブ切り替え時の自動ポーズ**。**コンティニュー機能**（ゲームオーバー画面で倒れたステージの最初からやり直せる）／**音あり・音なしの切り替え**（Mキー・ポーズ画面のボタン。設定はブラウザに保存）／**タブ切り替え時の自動ポーズ**（キーの押しっぱなしバグも修正）。その前は**見た目・演出の総仕上げ**＝改良42〜52。WARNING演出／ボスHPバー本格化（ボス名つき）／アイテム取得の手応え（光の輪＋「+100」）／敵弾を光る弾に＆ボスごとに色分け／爆発を丸い火花に／**ポーズ機能**（P・Esc・スマホ右上ボタン／音楽も止まる）／自機のエンジン噴射／ボス撃破の大演出／ボム発動のド派手化／タイトル画面の作り込み（ゴリラ＋自機）／背景の飾りを立体的に／**被弾時の赤フラッシュ**。すべて公開済み。直前：スピードアップアイテム「バナナ」／ステージ5「宇宙」で全5面完成。※今後マニュアルのPDF化は不要＝`manual.html`だけ直せばよい。※**デプロイの最後のステップがたびたび一時失敗する→空コミットで再push**（詳細は改良52の下の注意書き））

---

## 1. このプロジェクトは何か

Webブラウザで遊べる、クラシックな**縦スクロール・シューティングゲーム**です。
ポートフォリオ（自分の作品）として公開することがゴール。

- **ゲームタイトル**：**「ゴリラ、また来た。」**（各ステージのボスが全部ゴリラなのをネタにしたコミカルなタイトル）
  - 表示している場所：ゲームのタイトル画面（`src/main.ts`）・ブラウザのタブ名（`index.html`）・説明書（`public/manual.html`／`manual.pdf`）・`README.md`

- **作業フォルダ**：`/Users/yuichi/Claude_ゲーム`
- **公開URL（誰でも遊べる）**：https://yuichi0306.github.io/vertical-shooter/
- **GitHub**：ユーザー `yuichi0306` / リポジトリ `vertical-shooter`

---

## 2. 今の状態（2026-06-27時点）

**v1（最初の完成版）は完成・公開済み。** その後、見た目・音・ステージ・スマホ対応・説明書まで改良を重ねています。
**ソースコードの変更はすべてGitHubへプッシュ済みで、公開サイトにも反映されています。**
（※この `引き継ぎ書.md` 自体はGitに記録していない手元メモです）

### v1で完成している機能
- キーボード操作（移動：矢印/WASD、ショット：Z/スペース、ボム：X/Shift）
- 1ステージ ＋ ボス戦
- 残機制（ライフ3）＋ 被弾後の無敵時間
- アイテムによるパワーアップ（被弾で1段階ダウン）
- 円どうしの当たり判定
- タイトル → プレイ → ゲームオーバー／クリア の画面遷移
- ハイスコアのブラウザ保存
- 効果音（音声ファイル不要・その場で生成）と爆発エフェクト

### v1のあとに追加した改良
1. **ボス被弾音**：ボスに弾が当たるたびに、少し低めの「コッ」という音が鳴る
2. **ボス撃破音**：倒した瞬間に、雑魚とは違う派手な大爆発音（4つの音を重ねた迫力ある音）
3. **自機の姿変更**：上向き三角形 → **「頭がキリン・体が亀」**のふしぎな生きもの
4. **首の傾き**：左に移動すると首が左へ、右に移動すると右へ、なめらかに傾く
5. **アイテムの姿変更**：青い四角の「P」→ **キャベツ**の絵
6. **発射音の段階変化**：パワーアップ段階に応じて発射音が太く・派手になる（1=ピッ / 2=ピュン / 3=ドュン）

### さらに追加した改良
7. **敵を3種類に作り変え**（四角 → それぞれの生きものの絵）
   - 🐌 **カタツムリ**（`snail`）…ほとんど動かず、ゆっくり這って下りる
   - 🐍 **蛇**（`snake`）…横にくねくね動きながら下りる
   - 🕷️ **蜘蛛**（`spider`／新規追加）…糸で上下にビヨンと伸び縮みしながら（縦に動いて）下りる
8. **ボスの姿変更**：紫の四角 → **腕と足が動くゴリラ**（移動中ずっと、腕と足が交互に振れて歩いて見える）

### さらに追加した改良
9. **BGM（背景音楽）**：音声ファイル不要・その場で生成。**通常BGM**と**ボス戦BGM**があり、ボス登場で自動で切り替わる。決着（ゲームオーバー／クリア）で止まる。タイトルは無音
10. **蛇だけが弾を撃つ**：蛇（`snake`）は画面内にいる間、約1.8秒ごとに**自機を狙って弾を発射**。カタツムリと蜘蛛は撃たない
11. **ボスの怒りモード**：HPが**半分以下**になると一度だけ突入。攻撃間隔が短くなり＋自機狙いの弾を追加、左右移動も高速化。見た目は**赤いオーラが脈打ち、目が赤く光る**

### さらに追加した改良
12. **背景をにぎやかに（多層パララックス）**：星だけ → 奥から「夜空のグラデーション → 遠い山 → 星 → 流れる雲 → 近い地面の島」の5層。手前ほど速く流れて奥行きが出る
13. **ボム（緊急回避）**：`X`／`Shift`で発動。画面内の雑魚を一掃＋飛んでいる敵弾を全消去＋約1.5秒無敵。ボスには大ダメージ。**回数制（初期3発・自動回復なし）**。発動時は白い閃光＋衝撃波の輪＋専用効果音。残数はHUDの `Bomb:` に表示

### さらに追加した改良
14. **被弾時の演出強化**：やられた瞬間に**画面が揺れる（スクリーンシェイク）**＋**一瞬動きが止まる（ヒットストップ）**。揺れるのはゲーム世界（敵・自機・弾）だけで、背景とHUD・決着表示は揺らさない。最後の1機を失った時は揺れが大きくなる

### さらに追加した改良（最新）
15. **ステージ2を追加（連戦化）**：1面のボスを倒すと、そのまま2面が始まる。残機・パワー・ボム・スコアは引き継ぎ。最後（2面）のボスを倒すと「STAGE CLEAR!」。ステージ開始時に「STAGE 1」「STAGE 2」のバナーが出る
    - **背景**：2面は「地球の空」テーマ。水色のグラデーション＋白い雲＋緑の島（砂浜つき）＋海面のきらめき
    - **敵**：2面はカタツムリ無し。蛇・蜘蛛は続投。さらに🦅**鷲（`eagle`）**を追加…ふらふらランダムに動き回りながら自機へ弾を撃つ（少し硬く2発で撃破）
    - **ボス**：2面は🤖**機械のゴリラ（`machineGorilla`）**。1面ゴリラより硬く（HP120）、攻撃が多彩（自機狙い5発／扇／全方位リング／両腕の2列連射／回転うずまき弾）。左右だけでなく**前後（上下）にも動き**、自機に近づいたり離れたりする。見た目は金属ボディ＋光るバイザーの目・胸のコア・砲口のこぶし。怒りモードでは赤く光る

### さらに追加した改良（最新）
16. **スマホ対応（タッチ操作）**：タッチ端末では画面下に操作ボタンが出る。**左下＝移動パッド**（指を倒した向き・量で移動）、**右下＝ショットボタン**（押している間ずっと連射）と**ボムボタン**（残数表示つき）。タイトル/決着画面は**画面タップ**で進む。パソコンのキーボード操作はそのまま使える（タッチ端末でだけボタンが表示される）。画面は縦横どちらにも収まるよう自動で拡大、ピンチズーム/スクロールは無効化

### さらに追加した改良（最新）
17. **2面専用BGM（颯爽イメージ）**：ステージごとにBGMを設定できるようにし、2面に**颯爽とした疾走感の道中曲（`normal2`）**と**勇ましいボス曲（`boss2`）**を追加。1面は従来どおり（`normal`／`boss`）。曲はステージのデータ（`stage.ts` の `normalMusic`／`bossMusic`）で指定する

### さらに追加した改良（最新）
18. **レア敵「黄金の豚」（`goldpig`）**：各ステージに**1匹だけ**出現（タイムラインに1つだけ置いてある）。金色の豚が画面を横切るように飛ぶ。少し硬い（5発）。**倒すと必ずボム補充アイテムを落とし**、得点は通常の2倍（200点）。逃げられる前に倒すとお得
19. **スコア減点ルール**：
    - **ボムを1回使うごとに −200点**（その場で減算）
    - **クリア時、残機が満タン（3）から1減るごとに −500点**（残機を多く残すほど高得点）。クリア画面に「残機減点 −◯◯」と表示
    - ※スコアは0未満にはならない（0で止まる）

### さらに追加した改良（最新）
20. **プレイヤー向け「あそびかた（説明書）」**：ゲーム画面の**右上に「📖 あそびかた」リンク**を設置。押すと、ゲームと同じ配色の**説明書ページ**（`public/manual.html`）が別タブで開く。そこから**配布・印刷用のPDF**（`public/manual.pdf`・A4・日本語・白基調）もダウンロードできる
    - 公開先：説明書ページ `…/vertical-shooter/manual.html`、PDF `…/vertical-shooter/manual.pdf`
    - 元の文章（下書き）は手元の `説明書.md`（Git未登録のメモ）

### さらに追加した改良（最新・見た目の磨き込み）
21. **見た目の改善（プロのデザイナー目線）**：
    - **緑の交通整理**：夜空テーマの「近い島」を緑→**青グレー＋半透明**にして奥の飾りに下げた。緑色は「敵（蛇）・アイテム（キャベツ）・自機（亀）」だけのものにして見分けやすく（`drawGround`）
    - **弾を光らせる**：細い黄色い棒 → **丸いカプセル＋白い芯＋ふんわり光る**エネルギー弾に（`render()` 内「自機のショット」）
    - **HUDを角丸パネル化**：左上の表示を半透明の角丸パネルにまとめ、残機は**ハート ♥**、パワーは**3段バー**、ボムは**黄色い丸**に
    - **背景の奥行き**：夜空の遠い山を少しぼかして遠近感を強化（`drawBackground` の `ctx.filter`）
    - **タイトルの仕上げ**：文字裏の**暗がり（ビネット）＋緑のグロー**、スタート案内の**点滅**を追加（`render()` のタイトル部分）
22. **UIの調整**：
    - **プレイ中は右上の「📖 あそびかた」リンクを隠す**（タイトル・決着画面では表示）。切り替えは `render()` 冒頭の `helpLink.style.display`
    - **HUDパネルをふた回り小さく**してボス戦で邪魔にならないように（`render()` の `hudW`／`hudH` など）
23. **黄金の豚の体力を6→5発に変更**（`GOLDPIG_HP`）

### さらに追加した改良（最新）
24. **カタツムリ・蛇・蜘蛛に動きを追加**：各敵の描画に生存時間（`age`）を渡してアニメ化（鷲・豚と同じ仕組み）
    - 🐌 カタツムリ…体が前後に伸び縮みして這う＋触角がゆらゆら（`drawSnail`）
    - 🐍 蛇…胴体がうねうね波打つ＋頭が上下に揺れる＋舌をチロチロ出し入れ（`drawSnake`）
    - 🕷️ 蜘蛛…8本の脚がワサワサ動く＋糸で体がゆらゆら揺れる（`drawSpider`）
    - 揺れ幅を変えたいときは各関数内の数値（カタツムリ `sway`、蛇 `w`、蜘蛛 `wig` など）を増減
25. **ステージ2の島が重ならないように修正**：地球の空テーマの緑の島を、夜空と共用の `nearGround` 層から分離し、専用の `islands` 層に。**縦に等間隔・全部同じ速さ**で流す「ベルトコンベア方式」にして、陸地どうしが追いついて重なるのを防止
    - 数値の調整：`ISLAND_COUNT`（島の数）／`ISLAND_GAP`（縦の間隔）／`ISLAND_SPEED`（流れる速さ）
    - 夜空テーマ（1面）の島は従来どおり `nearGround` 層（`drawGround`）のまま

### さらに追加した改良（最新）
26. **ステージ3「海の中」を追加（連戦に3面目）**：2面のボスを倒すとそのまま3面へ。最後（3面）のボスを倒すと全クリア
    - **背景**：新テーマ `sea`。藍のグラデーション＋水面から差し込む光のすじ（`drawLightRays`）＋立ちのぼる泡（`bubbles`／`updateBubbles`）＋奥に沈む岩（`drawSeaRock`）
    - **敵**：
      - 🌸 クラゲ(`jellyfish`)…蛇と同じ動き（くねりながら弾を撃つ）。描画 `drawJellyfish`
      - 🐟 マグロ(`tuna`)…まっすぐ下りるが速い（黄金の豚の2倍速＝`TUNA_SPEED`）。描画 `drawTuna`
      - 🦑 イカ(`squid`)…鷲と同じ動き（ふらふら動いて撃つ）。描画 `drawSquid`
      - 🐷 黄金の豚…他ステージと同じ（1匹だけ）
    - **ボス**：🦍 酸素ボンベのゴリラ(`scubaGorilla`／`drawScubaGorilla`)。機械ゴリラの動き＋**たまに自機へ突進**（`updateBossCharge`：ためる→突っ込む→戻るの3段階。突進中の接触でダメージ）。設定は `BOSS_CONFIG.scubaGorilla`（`maxHp`／`chargeInterval`／`chargeSpeed` など）
27. **ステージ3に専用BGMを追加**：道中(`normal3`)＝優雅でゆったり、ボス(`boss3`)＝コミカルで弾む。曲ごとにメロディの音色を選べる `leadWave` を `audio.ts` の `TrackDef` に追加
28. **クラゲのまわりのピンクの円（オーラ）を削除**（`drawJellyfish` から）
29. **ステージ2にお助けキャラ「エンジェル」(`angel`)を追加**：黄金の豚と同じく画面を横切りつつ上下にも大きく動く。**触れると残機(ハート)が1増える**（上限 `MAX_LIVES`＝5・回復音 `playHeal`）。倒す相手ではないので**弾は素通り・ボムでも消えない・接触してもダメージなし**。描画 `drawAngel`、速さ等は `ANGEL_SPEED`／`ANGEL_Y`／`ANGEL_AMP`

### さらに追加した改良（最新）
30. **パワーアップを3段階→5段階に拡張**（`POWER_MAX`＝5）：キャベツ🥬を拾うほど強くなる。発射の中身は `fireShot()`（`src/main.ts`）。各段階の弾は次のとおり。
    - 1段階：正面に1発／2段階：横に2発／3段階：前方3方向（正面＋左右ななめ）
    - **4段階：前方に3方向 ＋ 後方に2方向**（前も後ろも撃てる。下から来る敵に後方ショットが効く）
    - **5段階：4段階の本体ショット ＋ お供の小亀（オプション）が前後（上下）にレーザー**（最強）
31. **5段階目「お供の小亀（オプション）」**：パワー5になると自機の少し左上に小さな亀が出現し、移動するとなめらかに**ついてくる**（トレイル追従）。ショットのたびに小亀の位置から**前後（上下）にまっすぐレーザー**を撃つ（敵・ボスにダメージ）。被弾でパワーが下がると消える。
    - 状態：`optionTurtle`（`x`／`y`／`active`）。定位置・追従の調整は `OPTION_OFFSET_X`／`OPTION_OFFSET_Y`／`OPTION_FOLLOW`、レーザーの速さは `LASER_SPEED`
    - 弾の型 `Bullet` に `laser?` を追加。`laser` の弾は水色の光の柱で描画（`render()` の弾ループ）。下向きの弾・レーザーが画面下に溜まらないよう、弾の消去に「下に出たら消す」(`b.y > HEIGHT + 10`)を追加
    - 小亀の絵は `drawMiniTurtle(cx, cy)`（自機の亀を小さくしたもの）
32. **ステージ2以降に「下から上ってくる敵」を追加**：出現データ（`stage.ts` の `timeline`）の各イベントに `from: "bottom"` を付けると、その敵は**画面の下から上へ**向かって進む（省略＝従来どおり上から）。
    - 仕組み：敵に進む向き `dir`（+1＝下へ／-1＝上へ）を持たせ、`spawnEnemy(kind, x, from)` で下出現なら画面下の外に置く。種類ごとの移動量に `dir` を掛けて反転（`update()` の「敵の移動」）。画面の**上に抜けた敵も消す**処理を追加
    - 見た目：上る敵は進行方向（上）を向くよう絵を**上下反転**（`drawEnemy` 内）。ただしクラゲ・イカは元から上向きが自然なので反転しない
    - 配置：ステージ2に下からの蛇・鷲、ステージ3に下からのマグロ・イカ・クラゲを `timeline` に追加済み。増やしたいときは `from: "bottom"` のイベントを足すだけ。※速いマグロを真下＝中央から出すと避けにくいので、中央は避けて配置すると親切
33. **キャベツ（パワーアップ）の出現率を調整**：`ITEM_DROP_RATE`（`src/main.ts`）＝**0.12（12%）**。黄金の豚以外の敵を倒すたびにこの確率でキャベツを落とす（黄金の豚は別ルールで必ずボムを落とす）

### さらに追加した改良（最新）
34. **ゲームタイトルを「ゴリラ、また来た。」に決定**：タイトル画面の文字（`src/main.ts` の `render()` タイトル部分）・ブラウザのタブ名（`index.html`）・説明書（`public/manual.html`／`manual.pdf`）・`README.md` をすべてこの名前に統一。各ステージのボスが全部ゴリラなのをネタにしたコミカルなタイトル
    - タイトル文字は末尾の句読点（、。）ぶん右へ少しずらして**見た目の中央**に揃えている（`punctNudge`）
35. **タイトル画面に壮大なBGMを追加**：スターウォーズ風のファンファーレ（金管っぽい `sawtooth` のメロディ＋どっしりした低音）。曲データは `audio.ts` の `TITLE_BASS`／`TITLE_LEAD`、`TRACKS` の `title`。`src/main.ts` のタイトル分岐で `setMusic("title")` を呼んで再生
    - **ブラウザの制限対応**：音は最初の操作まで鳴らせないので、**最初の1回の操作は「音を鳴らす合図」**にして（`audioReady` フラグ＋`titleLock=0.6`）、**もう一度押すとゲーム開始**。これで開始画面でも曲が聴ける。2回目以降のタイトル（ゲーム後に戻ったとき）は音が有効なので1回でスタート
    - 曲を変えたいとき：メロディは `TITLE_LEAD`（数字＝音の高さ）、速さは `title` の `stepDur`、音色は `leadWave`／`bassWave`
36. **弾の発射音を小さく調整**：`audio.ts` の `playShot()` の各 `tone(...)` の音量（後ろから2番目の数字）を 2段階下げた（元 `0.05` → `0.017` 前後）。連射でうるさくならないように。もっと上げ下げしたいときはこの数値を変える

### さらに追加した改良（最新）
37. **ステージ4「氷の世界」を追加（連戦に4面目）**：3面のボスを倒すとそのまま4面へ。最後（4面）のボスを倒すと全クリア（※最終ステージは将来「ステージ5＝宇宙」にする方針。4面はまだ最終ではない・3面より少し手ごわい程度）
    - **背景**：新テーマ `ice`。藍→白のグラデーション（`skyGradientIce`）＋ゆらめく**オーロラ**（`drawAurora`）＋奥の**氷山**（`drawIceberg`・`farHills`層を流用）＋手前の**雪の丘**（`drawSnowHill`・`nearGround`層）＋**降る雪**（`snowflakes`／`updateSnow`／`drawSnowflake`）
    - **敵（新規4種・動きは既存を流用）**：
      - ❄️ つらら(`tsurara`)…マグロと同じ（まっすぐ速い）。描画 `drawTsurara`
      - 🐧 ペンギン(`penguin`)…蛇と同じ（くねって雪玉を撃つ）。描画 `drawPenguin`
      - 🐻‍❄️ シロクマ(`shirokuma`)…鷲と同じ（ふらふら撃つ）だが硬い（`SHIROKUMA_HP`＝**3発**）。描画 `drawPolarBear`
      - ⛄ 雪だるま(`yukidaruma`)…カタツムリのようにゆっくり下りつつ、たまに撃つ（`YUKIDARUMA_HP`＝2／`YUKIDARUMA_SPEED`／`YUKIDARUMA_FIRE_INTERVAL`）。描画 `drawSnowman`
      - エンジェル・黄金の豚も登場（各1匹）
    - **ボス**：🦍 スノーゴリラ(`snowGorilla`／`drawSnowGorillaBoss`)。白いモコモコ＋つららの牙。設定は `BOSS_CONFIG.snowGorilla`（`maxHp`=165・突進あり）。新技「**つらら降らし**」(`bossPatternIcicles`：画面の上のあちこちから真下へ弾)
    - **BGM**：道中 `normal4`（幻想的）／ボス `boss4`（勇ましい）。`audio.ts` に追加
38. **雪だるまは倒すと爆発する**：雪だるま(`yukidaruma`)を倒すと白い爆風が出て、**当たり判定（`ENEMY_RADIUS`）の1.5倍＝約21px**の範囲に自機がいれば被弾する（近くで倒すと危ない）。**無敵中はノーダメージ**。仕組みは `snowmanExplode(x, y)`（`src/main.ts`）。弾で倒したとき・ボムで倒したときの両方で発生（ボム中は無敵なので実害なし）。範囲を変えたいときは `ENEMY_RADIUS * 1.5` の係数をいじる

### さらに追加した改良（最新・最終ステージ）
39. **ステージ5「宇宙」を追加（最終ステージ・全5面完成）**：4面のボスを倒すとそのまま5面へ。最後のラスボスを倒すと全クリア＝特別エンディング。シリーズ最難関。
    - **背景**：新テーマ `space`。漆黒のグラデーション(`skyGradientSpace`)＋カラフルな**星雲**(`drawNebula`)＋きらめく星(時間でちらつく)＋奥に浮かぶ**惑星**(`drawPlanet`・土星リングつきも・`farHills`層を流用)＋ななめに流れる**流れ星**(`drawComet`・`clouds`層を流用)
    - **敵（新規4種・動きは既存を流用）**：
      - 🛸 UFO(`ufo`)…シロクマと同じ（ふらふら撃つ）だが硬い(`UFO_HP`＝**3発**)。描画 `drawUfo`
      - ☄️ 隕石(`meteor`)…マグロと同じ（まっすぐ速い）。描画 `drawMeteor`
      - 👾 エイリアン(`alien`)…蛇と同じ（くねって撃つ）。描画 `drawAlien`
      - 🛰️ 衛星ロボ(`satellite`)…雪だるまと同じ（ゆっくり＋たまに撃つ）。**倒すと爆発・範囲は雪だるまの2倍**(`SATELLITE_BLAST_RADIUS`＝`ENEMY_RADIUS*3.0`)。描画 `drawSatellite`。HP=`SATELLITE_HP`(2)
      - エンジェル・黄金の豚も登場（各1匹）
    - **🆕中ボス「宇宙チンパンジー」(`spaceChimp`)**：道中の途中(`midBossTime`＝11秒)で登場。**HPバーが出て、倒すまで道中が停止**（タイムラインの時間が止まる）→ 撃破で道中再開→やがてラスボス。ボスの仕組みを流用した小さめボス（`BOSS_CONFIG.spaceChimp`・HP95）。BGMはボス曲(`boss5`)を流用。描画 `drawSpaceChimpBoss`。新システムの仕組み：`spawnBoss(kind, isMid=true)`／`Boss` に `isMid` を追加／`midBossSpawned` フラグ／`defeatBoss()` は `isMid` なら次ステージへ進まず道中再開
    - **👑ラスボス「メカ母艦ゴリラ」(`motherGorilla`)**：2面の機械ゴリラの最終進化版（巨大戦艦と一体化）。シリーズ最高HP(`BOSS_CONFIG.motherGorilla`・HP260)＋今までの全技＋突進。**戦闘中、お供「小型メカゴリラ」(`minionGorilla`)を射出**（`spawnMinion`／`Boss.summonTimer`／間隔 `SUMMON_INTERVAL`・怒り時 `SUMMON_INTERVAL_ENRAGED`・同時上限 `MINION_MAX`=3）。描画 `drawMotherGorillaBoss`・お供は `drawMinionGorilla`
    - **🏆特別エンディング**：ラスボス撃破後、`gameState==="clear"` で `drawEndingScreen()` を表示。「ゴリラ完全制覇！」＋**歴代ボス5体（ゴリラ→機械→酸素ボンベ→スノー→メカ母艦）が勢ぞろい**＋「…ぜんぶゴリラだった。」のオチ＋最終スコア。`render()` の決着画面はクリア時だけ専用画面、ゲームオーバーは従来表示
    - **BGM**：道中 `normal5`（神秘的）／ボス `boss5`（壮大な最終決戦）。`audio.ts` に追加
    - **スコア**：中ボス撃破 `SCORE_MIDBOSS`＝2000点
    - **全クリアのお祝いBGM**：`audio.ts` に `ending` トラック（ハ長調 C-G-Am-F の明るい曲）を追加。`defeatBoss()` の全クリア処理で `setMusic("ending")`（従来は無音だった）
    - **中ボス撃破後の延長**：`STAGE5` の `duration` を 33 にし、22〜31秒に第2の山場（敵ウェーブ）を追加。中ボス〜ラスボスをたっぷり戦わせる

### さらに追加した改良（最新）
40. **スピードアップアイテム「バナナ」(`speed`)を追加**：拾うと自機の移動が速くなる（`src/main.ts`）。
    - **各ステージに2個・道中のランダムな時刻に出現・ボス戦中は出ない**：出現予定は `scheduleBananas()`（`resetGame`/`advanceStage` で `bananaTimes` に2つの時刻を抽選）。`update()` の道中処理内で `!boss` のときだけ `spawnBanana()`（中ボス戦中は道中の時間が止まるので自然に出ない）
    - **最大2段階**（`SPEED_MAX`＝2）：1段階ごとに移動速度+20%（`SPEED_STEP`）。実際の速さは `player.speedLevel` を `PLAYER_SPEED * (1 + speedLevel*SPEED_STEP)` に反映。`player.speedLevel` は被弾では下がらず、`resetGame` で0に戻る（ステージをまたいでも引き継ぐ）
    - **絵**：`drawBanana(cx, cy)`。アイテム描画の振り分けは `render()` の `it.kind === "speed"`。`Item` 型に `"speed"` を追加
    - **HUD表示**：残機の右に「SPD」インジケーター（`SPEED_MAX` ぶんの小さなマス。`render()` のHUD）
41. **強化アイテム取得で+100点**：キャベツ（`power`）とバナナ（`speed`）を拾うと `SCORE_ITEM`（=100）加点（`update()` のアイテム取得処理）。ボム補充（`bomb`）は加点なし

### さらに追加した改良（最新・見た目/演出の総仕上げ）
42. **UI演出を強化**（`src/main.ts` の `render()`）：
    - **WARNING演出**：ボス入場中の表示を、赤く点滅する警告バー＋上下を流れる黄×黒のハザード縞＋「⚠ WARNING ⚠／BOSS APPROACHING」に（`render()` の「WARNING表示」）
    - **ボスHPバーを本格化**：角丸＋ツヤ（ボス色の縦グラデ）＋10目盛り＋バー下に**ボス名**を表示。名前は `BOSS_NAME`。中ボスは「中ボス ○○」、ラスボスは「★ラスボス★ ○○」
    - **開始バナー**（「STAGE 1」など）：左からスッと滑り込み→止まる→右へ抜ける＋上下の飾りライン＋「START!」。テーマ色で色替え
    - **GAME OVER画面**：赤いビネット＋赤く光る大文字＋区切り線＋新記録は金色に点滅
43. **アイテム取得の手応え**：拾った瞬間に**光の輪**（`rings`／`spawnRing`）＋キラキラ＋**「+100」などの浮かぶ文字**（`floatTexts`／`spawnFloatText`）を表示。キャベツ・バナナ＝「+100」、ボム補充＝「BOMB +1」、エンジェル＝「1UP!」。`update()` のアイテム取得・エンジェル処理／描画は `render()`。`resetGame` でクリア
44. **敵の弾を光る弾に＋ボスごとに色分け**：敵弾（`EnemyBullet`）に `color` を追加。ふつうの敵＝赤（`EBULLET_DEFAULT_COLOR`）、ボスは `BOSS_BULLET_COLOR`（ゴリラ=橙/機械=水色/酸素=緑/雪=白青/中ボス=紫/ラスボス=金）。ボスのパターン発射時だけ `currentEBulletColor` を切り替える。描画はオーラ＋白い芯＋脈打ち（`render()`）。色の透明度計算に `hexToRgba()`
45. **爆発の破片を丸い火花に**：`Particle` に `size` を追加し、四角ドット→縮みながら消える丸い発光火花に（`spawnExplosion`／`render()`）
46. **ポーズ機能**：`GameState` に `"paused"` を追加。**P / Esc**（`keydown`）または**スマホ右上の「Ⅱ」ボタン**（`PAUSE_CX/CY/R`・`touchstart`）で一時停止／再開。`pauseGame()`／`resumeGame()`（**ポーズ中は音楽も止め**、再開で今の場面のBGMを鳴らし直す）。`frame()` はポーズ中 `update` を止めて描画だけ。PAUSE画面は `render()`。説明書・タイトルの操作説明にも記載
47. **自機のエンジン噴射（トレイル）**：自機の後ろに光の粒（`trails`／`Trail`）を出す。動くと勢いが増す。テーマ色は `exhaustColor()`（発光合成 `lighter` で描画）。`update()` の移動処理で発生、`resetGame` でクリア
48. **ボス撃破の大演出**：`defeatBoss()` で画面フラッシュ（`bossDefeatFlash`／`BOSS_DEFEAT_FLASH_TIME`・`bossDefeatColor`）＋三重の衝撃波リング＋大量の破片＋大きめの揺れ（`shakeTime`）＋一瞬のヒットストップ（`hitStop`）。フラッシュ描画は `render()`
49. **ボム発動をド派手に**：`useBomb()` で自機中心の放射状フラッシュ＋三重の衝撃波リング＋キラキラ＋画面揺れ。フラッシュ描画は `render()` の「ボムの閃光」
50. **タイトル画面を作り込み**：大きな**ゴリラのマスコット**（`drawGorillaBoss` を拡大・歩き＋上下に揺れる）＋下に**自機（キリン亀）がゴリラを見上げて待機**（`drawPlayerGiraffeTurtle` を拡大・首を振る）。`render()` のタイトル分岐
51. **背景の飾りを立体的に**：平たい単色オーバルにグラデ＋陰影を追加。夜空＝`drawHill`／`drawGround`（縦グラデ＋上ふちハイライト）、空＝`drawIsland`（ドーム状の放射グラデ）、海＝`drawSeaRock`（縦グラデ＋リムライト）、氷＝`drawIceberg`（影の面で3D化）／`drawSnowHill`（グラデ）。宇宙の `drawPlanet` は元からグラデ付き
52. **被弾時の赤フラッシュ**：やられた瞬間に**画面のフチが赤くにじんでスッと消える**（中央は透明のままで操作の邪魔にならない）。状態は `hurtFlash`／表示時間 `HURT_FLASH_TIME`。発動は `damagePlayer()`、描画は `render()` の「被弾フラッシュ」（ゲーム世界の揺れとは別レイヤーで画面端に固定）、`resetGame` でリセット。既存の揺れ（`shakeTime`）・ヒットストップ（`hitStop`）に加わる

### さらに追加した改良（最新・遊びやすさの改善）
53. **コンティニュー機能**：ゲームオーバー画面に2つのボタンを表示。**「▶ コンティニュー」＝倒れたステージの最初からやり直し（スコアは0に戻る・残機/パワー/ボムも初期値）**、「タイトルへ」＝従来どおり。キーは **Z＝コンティニュー / X＝タイトルへ**、スマホはボタンをタップ。仕組み：`resetGame(startStage)` に引数を追加し、コンティニューは `resetGame(stageIndex)` を呼ぶだけ。タイトルへ戻る処理は `backToTitle()` に共通化。ボタンの位置は `GO_BTN_*`／`GO_CONT_Y`／`GO_TITLE_Y`、タップの受け付けは `touchstart` の `gameover` 分岐（`goTap`）。ボタンの絵は共通関数 `drawUiButton()`
54. **音あり／音なしの切り替え（ミュート）**：**Mキー（どの画面でも）**または**ポーズ画面の「🔊音あり/🔇音なし」ボタン**で切り替え。設定は `localStorage`（キー `vshooter.muted`）に保存され**次回も引き継がれる**。仕組み：`audio.ts` に全ての音の親つまみ `masterGain` を追加（効果音もBGMも全部ここを通る）。`setMuted(true/false)`／`isMuted()` を export。`main.ts` 側は `toggleMute()`（保存も担当）。ポーズ画面のボタン位置は `MUTE_BTN_*`
55. **タブ切り替え時の自動ポーズ＋キー押しっぱなしバグ修正**：タブや別ウィンドウへ切り替えると「キーを離した」信号が届かず、戻ったとき勝手に動き続けるバグがあった。画面が隠れたら（`visibilitychange`／`blur`）①押されている扱いのキー・指を全部リセット ②プレイ中なら自動でポーズ、するようにした（`onScreenHidden()`）。おまけ：バックグラウンドでBGMが乱れる問題も自動ポーズで解消
56. **Retina対応（高精細画面のにじみ解消）**：キャンバスの内部解像度を「実際の表示サイズ×ピクセル密度（devicePixelRatio）」まで上げた。座標系はこれまでどおり480×640のまま（`render()` の最初の `ctx.setTransform` で拡大）。仕組みは `resizeCanvasForScreen()`（起動時と画面サイズ変更時に呼ばれる）。`index.html` の `image-rendering: pixelated` は削除（この絵柄には逆効果だった）
57. **背景ぼかしのキャッシュ化（軽量化）**：毎フレーム `ctx.filter="blur()"` をかけていた山（夜空）・岩（海）・氷山（氷）・惑星（宇宙）を、起動時に1回だけ「ぼかし済みの絵」として別キャンバスに焼き込み、毎フレームは貼るだけにした。仕組み：`bakeBlurredSprite(half, blur, draw)`（焼き込み）と `drawBaked(sprite, d)`（貼り付け）。形の定義は `drawHillShape`／`drawSeaRockShape`／`drawIcebergShape`／`drawPlanetShape`（旧 `drawHill`／`drawSeaRock`／`drawIceberg`／`drawPlanet` は廃止。中心(0,0)に等倍で描く形式に変更）。焼き込み済みスプライトは `hillSprite`／`seaRockSprite`／`icebergSprite`／`planetSprites`（3色）
58. **キャベツの救済（天井）**：ドロップが12%の運まかせだと、運が悪いとパワー1のままボス戦になる。**8体倒してもキャベツが出なければ次は必ず出す**ようにした（`CABBAGE_PITY_KILLS`＝8／カウンターは `killsSinceCabbage`。キャベツが出るたび0に戻る・`resetGame` でもリセット）
59. **ポーズ画面から「タイトルへ戻る」**：ポーズ画面の音ボタンの下に「タイトルへ（T）」ボタンを追加（タップ or **Tキー**）。ボムのXキーと押し間違えて周回が消えないよう、キーはあえて離れたTにした。ボタン位置は `PAUSE_TITLE_BTN_Y`（音ボタンと同じ幅 `MUTE_BTN_*`）、キー処理は `keydown` のT分岐（ポーズ中のみ有効）

> ⚠️ **公開（デプロイ）の注意**：`git push` で GitHub Actions が自動ビルド＆公開する（`.github/workflows/deploy.yml`）。**最近このデプロイの最後の「deploy」ステップ（`actions/deploy-pages`）だけが一時的に失敗することがたびたび起きている**（`build` ジョブ＝ビルドは成功、`deploy` ジョブだけ失敗＝本番に反映されない。GitHub Pages 側の一時エラーでコードの問題ではない）。そのときは**空コミットを push して再実行**すればよい：`git commit --allow-empty -m "chore: 再デプロイ" && git push`。1回で直ることが多い。
> - **反映確認の手順**：①公開サイトの `index.html` が参照する `assets/index-*.js` のハッシュが、ローカル `dist/index.html`（`npm run build` 後）と一致するか。②`gh` コマンドは未インストールなので、Actions の成否は GitHub API で見る：`curl -s "https://api.github.com/repos/yuichi0306/vertical-shooter/actions/runs?per_page=1"`（`"conclusion"` が `success`/`failure`）。③ブラウザ側は強制リロード（Cmd+Shift+R）。

---

## 3. ファイルの中身（どこに何があるか）

| ファイル | 役割 |
|---|---|
| `src/main.ts` | ゲームの本体（ループ・自機・敵・ボス・当たり判定・描画すべて） |
| `src/stage.ts` | **ステージのデータ**（各面の背景テーマ・出現タイミング・ボスの種類）。`STAGES` 配列に並べた順番で連戦する |
| `src/audio.ts` | 効果音とBGM（プログラムで音を合成。音声ファイルは使っていない） |
| `index.html` | ゲームを表示するページの土台（右上の「あそびかた」リンクもここ） |
| `public/manual.html` | プレイヤー向け説明書ページ（公開サイトで `/manual.html`） |
| `public/manual.pdf` | 配布・印刷用の説明書PDF（公開サイトで `/manual.pdf`） |
| `README.md` | 企画の設計図（仕様・技術・ロードマップ） |
| `説明書.md` | 説明書の元文章（手元メモ・Git未登録） |

> 📁 `public/` フォルダの中身は、ビルド時に公開用フォルダ `dist/` の一番上へそのままコピーされます（だから `/manual.html` で開ける）。

### `main.ts` の中の「絵を描く」関数（見た目をいじりたいとき）
- `drawPlayerGiraffeTurtle(cx, cy, lean)` … 自機（キリン亀）。`lean` が首の傾き
- `drawCabbage(cx, cy)` … アイテム：キャベツ（パワーアップ）
- `drawBanana(cx, cy)` … アイテム：バナナ（スピードアップ）
- `drawBombItem(cx, cy)` … アイテム：ボム補充（黄金の豚が落とす）
- `drawSnail(cx, cy, age)` … 敵：カタツムリ（`age`で這う動き・触角の揺れ）
- `drawSnake(cx, cy, age)` … 敵：蛇（`age`で胴のうねり・舌の出し入れ）
- `drawSpider(cx, cy, age)` … 敵：蜘蛛（上に糸が伸びる。`age`で脚のワサワサ・糸の揺れ）
- `drawEagle(cx, cy, age)` … 敵：鷲（翼を羽ばたかせる。2面で登場）
- `drawGoldPig(cx, cy, age)` … 敵：黄金の豚（レア。きらめく）
- `drawJellyfish(cx, cy, age)` … 敵：クラゲ（3面。かさ＋触手）
- `drawTuna(cx, cy, age)` … 敵：マグロ（3面。まっすぐ速く泳ぐ魚）
- `drawSquid(cx, cy, age)` … 敵：イカ（3面。むらさきの足うねうね）
- `drawAngel(cx, cy, age)` … お助けキャラ：エンジェル（2面・4面・5面。触れると残機+1）
- `drawTsurara(cx, cy, age)` … 敵：つらら／氷の塊（4面。まっすぐ速く落ちる）
- `drawPenguin(cx, cy, age)` … 敵：ペンギン（4面。よちよち＋雪玉ショット）
- `drawPolarBear(cx, cy, age)` … 敵：シロクマ（4面。ふらふら撃つ・3発で硬い）
- `drawSnowman(cx, cy, age)` … 敵：雪だるま（4面。倒すと爆発）
- `drawUfo(cx, cy, age)` … 敵：UFO（5面。ふらふら撃つ・3発で硬い）
- `drawMeteor(cx, cy, age)` … 敵：隕石（5面。炎の尾を引いてまっすぐ速く落ちる）
- `drawAlien(cx, cy, age)` … 敵：エイリアン（5面。緑の宇宙人・くねって撃つ）
- `drawSatellite(cx, cy, age)` … 敵：衛星ロボ（5面。倒すと爆発・範囲は雪だるまの2倍）
- `drawMinionGorilla(cx, cy, age)` … 敵：小型メカゴリラ（5面・ラスボスのお供）
- `drawMiniTurtle(cx, cy)` … パワー5段階目の「お供の小亀」（自機の亀を小さくしたもの）
- `drawEnemy(e)` … 敵の種類に応じて上の絵を呼び分ける（下から上る敵 `dir<0` は上下反転して描く）
- `drawTouchControls()` … スマホ用の操作ボタン（移動パッド・ショット・ボム）
- `drawGorillaBoss(cx, cy, walk, enraged)` … 1面ボス（ゴリラ）
- `drawMachineGorillaBoss(cx, cy, walk, enraged)` … 2面ボス（機械ゴリラ）
- `drawScubaGorilla(cx, cy, walk, enraged)` … 3面ボス（酸素ボンベのゴリラ）
- `drawSnowGorillaBoss(cx, cy, walk, enraged)` … 4面ボス（白いモコモコのスノーゴリラ）
- `drawSpaceChimpBoss(cx, cy, walk, enraged)` … 5面・中ボス（宇宙服のチンパンジー）
- `drawMotherGorillaBoss(cx, cy, walk, enraged)` … 5面・ラスボス（メカ母艦ゴリラ）
- `drawEndingScreen()` … 全クリアの特別エンディング（歴代ボス5体が勢ぞろい）
- `drawNebula / drawComet(d)` … 宇宙テーマの星雲・流れ星（惑星は下記のぼかしスプライト方式）
- `drawBackground()` … 背景をテーマ（`night`=夜空／`sky`=地球の空／`sea`=海の中／`ice`=氷の世界／`space`=宇宙）で切り替えて描く
- `drawCloud / drawGround(d)` … 夜空テーマの雲・近い地面
- **奥のぼかし飾り（山・岩・氷山・惑星）はスプライト方式**：形は `drawHillShape / drawSeaRockShape / drawIcebergShape / drawPlanetShape`（中心(0,0)に等倍で描く）。起動時に `bakeBlurredSprite()` で「ぼかし済みの絵」に焼き込み、毎フレームは `drawBaked()` で貼るだけ（軽量化。改良57）
- `drawSeaSparkle / drawIsland / drawCloudWhite(d)` … 空テーマの海のきらめき・緑の島・白い雲
  - 空テーマの島は専用の `islands` 層（縦に等間隔・同じ速さで流れ、重ならない）。数値は `ISLAND_COUNT`／`ISLAND_GAP`／`ISLAND_SPEED`
- `drawLightRays / drawBubble(d)` … 海テーマの差し込む光・泡（奥の岩は上記のぼかしスプライト方式）
  - 泡は専用の `bubbles` 層（`updateBubbles` で下から上へ昇る）
- `drawAurora / drawSnowHill / drawSnowflake(d)` … 氷テーマのオーロラ・近い雪の丘・降る雪（奥の氷山は上記のぼかしスプライト方式）
  - 雪は専用の `snowflakes` 層（`updateSnow` で上から下へ降る）。氷山は `farHills` 層、雪の丘は `nearGround` 層を流用
- 弾やボムの閃光などは `render()` 関数の中で直接描いている

### 背景をいじりたいとき（`main.ts`）
- 各層の数・速さ・大きさは `makeLayer(...)` の呼び出し（`farHills` / `clouds` / `nearGround`）
- 空の色は `skyGradientNight`（夜空）／`skyGradientDay`（地球の空）／`skyGradientSea`（海の中）／`skyGradientIce`（氷）／`skyGradientSpace`（宇宙）
- 描き分けは `drawBackground()`。どの面でどれを使うかは各ステージの `theme`（`night` / `sky` / `sea` / `ice` / `space`）

### 敵の動き・攻撃を変えたいとき（`main.ts`）
- 速さの数値：`SNAIL_SPEED`（カタツムリ）／`ENEMY_SPEED`（蛇の降下）／`SPIDER_SPEED`（蜘蛛）
- 蛇のくねり幅・速さ：`SNAKE_AMPLITUDE` / `SNAKE_FREQ`、蜘蛛の上下：`SPIDER_FREQ`
- **蛇の弾の頻度**：`SNAKE_FIRE_INTERVAL`（小さいほど激しく撃つ）
- 動きと発射のロジックは `update()` の中の「敵の移動（種類ごとに動きが違う）」を参照

### 敵の種類・出し方を変えたいとき（`stage.ts`）
- どの面にどの敵をいつ出すかは各ステージの `timeline`（`STAGE1`〜`STAGE5`）
- **下から上らせたい**ときは、そのイベントに `from: "bottom"` を付ける（省略＝上から下りる）。例：`{ time: 5.5, kind: "tuna", x: 0.2, from: "bottom" }`
- 鷲の動き・弾は `main.ts` の `EAGLE_*` 定数（`EAGLE_WANDER_SPEED`＝ふらつく速さ、`EAGLE_FIRE_INTERVAL`＝弾の間隔、`EAGLE_HP`＝硬さ）

### ボスの強さを変えたいとき（`main.ts`）
- ボスごとの設定は `BOSS_CONFIG`（`gorilla` / `machineGorilla` / `scubaGorilla` / `snowGorilla` / `spaceChimp` / `motherGorilla`）にまとまっている
  …体力 `maxHp`／左右移動 `swaySpeed`／前後移動の幅と速さ `bobAmplitude`・`bobSpeed`（0なら前後移動しない）／攻撃間隔 `fireInterval`／怒り時の各値／使う技の一覧 `patterns`／突進の間隔と速さ `chargeInterval`・`chargeSpeed`（0なら突進しない）
- 怒りモードが始まるHPの割合：`BOSS_ENRAGE_RATIO`
- **中ボスとラスボスのお供（ステージ5）**：中ボスはステージに `midBoss`／`midBossTime` を設定すると出る（`Boss.isMid`・`defeatBoss()` で道中再開）。ラスボスのお供射出は `motherGorilla` 専用で `spawnMinion`／`summonTimer`（`SUMMON_INTERVAL`・`MINION_MAX`）
- 攻撃の技そのものは `bossPattern◯◯` 関数（新しく作って `BOSS_CONFIG` の `patterns` に足すと技が増える）

### ステージを増やしたいとき（`stage.ts`）
- `STAGE2` をまねて新しいステージを作り、`STAGES` 配列に並べるだけで連戦に追加できる
- 1ステージは「名前 `name`／背景 `theme`／ボス `boss`／道中の長さ `duration`／出現データ `timeline`」

### スマホのタッチ操作を変えたいとき（`main.ts`）
- ボタンの位置・大きさ：`PAD_*`（移動パッド）／`SHOT_*`（ショット）／`BOMB_*`（ボム）
- 見た目は `drawTouchControls()`、指の処理は `touchstart` / `touchmove` / `endTouch` のあたり
- タッチ端末かどうかの判定は `showTouchControls`（最初に自動判定＋初回タッチでオン）
- 画面の収まり方（拡大・ズーム無効）は `index.html` の `<style>` と viewport の設定

### パワーアップ（ショット）を変えたいとき（`main.ts`）
- 各段階の弾の出し方は `fireShot()`（`if (player.power >= 4) …` のように段階で分岐）。弾は `bullets.push({ x, y, vx, vy })` で出す（`vy` を＋にすると下＝後方へ飛ぶ）
- 最大段階は `POWER_MAX`（今は5）。キャベツの出現率は `ITEM_DROP_RATE`（今は0.12＝12%）
- お供の小亀（5段階目）：定位置・追従は `OPTION_OFFSET_X`／`OPTION_OFFSET_Y`／`OPTION_FOLLOW`、レーザーの速さは `LASER_SPEED`、絵は `drawMiniTurtle()`
- HUDのパワーバーは `POWER_MAX` の数だけ自動で増える（`render()` の `for (let i = 0; i < POWER_MAX; i++)`）

### スピードアップ（バナナ）を変えたいとき（`main.ts`）
- 速さの上がり幅：`SPEED_STEP`（1段階ごとに+20%）／最大段階：`SPEED_MAX`（今は2）。実際の移動は `moveSpeed = PLAYER_SPEED * (1 + player.speedLevel * SPEED_STEP)`
- 出現数・タイミング：`scheduleBananas()`（今は1ステージ2個・道中の2秒〜「duration-2秒」でランダム）。出すのは `spawnBanana()`、出現判定は `update()` の道中処理（`!boss` のときだけ）
- 絵は `drawBanana()`、HUDの「SPD」表示は `render()`（`SPEED_MAX` ぶんのマス）
- 取得時の加点は `SCORE_ITEM`（=100。キャベツも同じ）

### ボムの強さを変えたいとき（`main.ts`）
- `START_BOMBS`（初期の持ち数）／`BOMB_INVINCIBLE_TIME`（無敵の長さ）／`BOSS_BOMB_DAMAGE`（ボスへのダメージ）
- 発動の中身は `useBomb()` 関数

### 被弾の手応え（揺れ・止まり）を変えたいとき（`main.ts`）
- `SHAKE_TIME`（揺れの長さ）／`SHAKE_MAG`（揺れの大きさ）／`HITSTOP_TIME`（一瞬止まる長さ）
- 発動は `damagePlayer()`、揺れの描画は `render()` の「ゲーム世界」を囲む `ctx.save()〜restore()`、止まりは `frame()` のヒットストップ処理

### `audio.ts` の中の効果音・BGM（音をいじりたいとき）
- `playShot(power)` … 発射音（power=強化段階で音が変わる）
- `playExplosion()` … 雑魚の爆発音
- `playBossHit()` … ボス被弾音（少し低め）
- `playBossExplosion()` … ボス撃破の派手な大爆発音
- `playHit()` … 自機が被弾したときの音
- `playBomb()` … ボム発動の「ドゴォン」という音
- `playHeal()` … エンジェルに触れて残機+1のときの「キラッ」という回復音
- `setMusic("normal" | "boss" | "normal2"〜"normal5" | "boss2"〜"boss5" | "title" | "ending" | null)` … BGMの切り替え（`null` で停止）。`ending`＝全クリアのお祝い曲
- `setMuted(true/false)`／`isMuted()` … 音あり/音なしの切り替え（効果音もBGMも全部）。切り替え＋保存は `main.ts` の `toggleMute()` から呼ぶ
- BGMの曲（メロディ）は `NORMAL_BASS/LEAD`・`BOSS_BASS/LEAD`・`NORMAL2_*`〜`NORMAL5_*`・`BOSS2_*`〜`BOSS5_*` の数字の並び。各曲の波形（ベース `bassWave`・メロディ `leadWave`）と速さ（`stepDur`）は `TRACKS`、音量は `MUSIC_VOL`
- どのステージでどの曲を鳴らすかは `stage.ts` の各ステージの `normalMusic`／`bossMusic`

### 説明書（あそびかた）を直したいとき
- 文章・見た目：`public/manual.html` を編集（画面の `<a id="help-link">` の文言は `index.html`）
- **PDFは `manual.html` から作っています**。内容を直したらPDFも作り直す。作業フォルダで：
  ```bash
  "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" \
    --headless --disable-gpu --no-pdf-header-footer \
    --print-to-pdf="public/manual.pdf" "file://$(pwd)/public/manual.html"
  ```
  ※PDFは `manual.html` の `@media print`（印刷用の白基調スタイル）で作られます。色を変えたいときはそこを編集

---

## 4. 動かし方・公開のしかた

すべて作業フォルダ内のターミナルで実行します。

```bash
# 開発しながら確認する（保存すると自動でブラウザに反映）
npm run dev
# → 表示された http://localhost:5173/ などをブラウザで開く
# → 止めるときは Ctrl + C

# 公開用ファイルが作れるか確認（型チェックも兼ねる）
npm run build
```

### 変更を公開する手順（このプロジェクトの流れ）
1. コードを直す
2. `npm run build` でエラーが出ないか確認
3. コミットする（説明は `feat:` 新機能 / `fix:` 修正 で始める）
4. `git push` でGitHubへ送る → **自動で公開サイトに反映される**（1分ほど）

> **ターミナルから直接 `git push` できます。**（SSHキー設定済み。GitHub Desktopは不要だが使ってもOK）

---

## 5. 次にできること（アップデート候補）

README にもあるが、今後いじれるところ：

- ~~**ステージ5「宇宙」を追加して最終ステージにする**~~：✅ 完成（全5面・最終ステージ「宇宙」＝中ボス＋ラスボス＋特別エンディング）。さらにステージを増やすなら同じ手順で `STAGES` 配列に足せる（ただし現状は宇宙が最終決戦の想定）
- **全体のバランス調整（おすすめの次の一手）**：実際に全5面を通して遊んでみて、難易度カーブを整える。数値1つで変えられる主な所＝ラスボスHP `BOSS_CONFIG.motherGorilla.maxHp`(260)・中ボスHP `spaceChimp.maxHp`(95)・お供の間隔 `SUMMON_INTERVAL`／上限 `MINION_MAX`・各ステージの `duration`／`timeline`（`stage.ts`）
- **ボスの攻撃パターン追加**：`main.ts` の `bossPattern◯◯` 関数を作り、`BOSS_CONFIG` の `patterns` に足す
- **難易度カーブ**：各ステージの `timeline`（`stage.ts`）を調整
- **敵の種類をさらに増やす**：`stage.ts` の `EnemyKind` に種類を足し、`main.ts` の「敵の移動」と `drawEnemy` に動き・絵を追加
- **コンボ得点**：連続撃破でボーナス倍率アップなどのやり込み要素（未実装）
- **スコア調整**：減点ルールの数値は `main.ts` の `BOMB_PENALTY`（ボム減点）・`LIFE_PENALTY`（残機減点）。黄金の豚の強さ・速さは `GOLDPIG_HP`／`GOLDPIG_SPEED`
- ~~**スマホ対応（タッチ操作）**~~：✅ 対応済み（画面下のボタンで操作）
- **見た目の差し替え**：弾やアイテムなど、残りの図形もまだ絵にできる
- **プロ目線レビューの残り候補**（2026-07-04に検討。コンティニュー・ミュート・タブ切り替えバグ・Retina対応・ぼかしキャッシュ化・キャベツ救済は対応済み）：
  - **自機の「本当の当たり判定」の点を表示**：絵より小さい半径11pxの判定が見えないので、自機中心に小さな光る点を常時表示すると避けやすくなる
  - **ゲームパッド対応**（Gamepad API）
  - **main.ts の分割**（5,600行超。「敵」「ボス」「描画」くらいに分けると今後の改修が楽）

---

## 6. 大事なルール・注意点

- **ユーザーは非エンジニア**。専門用語にはかんたんな説明を添え、手順は1つずつ具体的に。
- 変更したら必ず `npm run build` でエラーが無いか確認してからコミットする。
- コミットメッセージは日本語、先頭に `feat:` / `fix:` を付ける（Conventional Commits）。
- `node_modules` と `dist` フォルダはGitに記録しない（自動生成のため `.gitignore` で除外済み）。
- `public/` フォルダ（`manual.html`／`manual.pdf`）は**Gitに記録する**（公開サイトで配るファイルのため）。
- `引き継ぎ書.md` と `説明書.md` は手元メモなので**Git未登録**のまま。
- 当たり判定（`PLAYER_RADIUS` など）は見た目を変えても**そのまま**にしておくと、遊びごこちが変わらない。
